const City = ({population, name}) => {
    return (
        <div>
            <p>{name}</p>
            <h3>POPULATION</h3>
            <p>{population}</p>
        </div>
    )
}

export default City
